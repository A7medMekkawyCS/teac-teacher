# Teac Logo E1 Implementation Plan

> **For agentic workers:** Implement task-by-task. Steps use checkbox syntax.

**Goal:** Ship E1 brand mark on Splash, Onboarding, Login, Role Select, and Student/Teacher/Parent Home.

**Architecture:** Serve `public/brand/logo.png`; rewrite `LogoMark`/`Wordmark` in `src/App.tsx` to use the asset; add missing placements on Onboarding + Student/Parent Home headers.

**Tech Stack:** React + Vite public assets

## Global Constraints

- Mark is E1 only (`logo.png`)
- Key screens only (spec placement table)
- Keep existing teal brand tokens
- Do not expand to every header/favicon set

---

### Task 1: Asset + LogoMark/Wordmark

- [x] Ensure `public/brand/logo.png` exists (E1)
- [x] Replace SVG `LogoMark` with `<img src="/brand/logo.png">`
- [x] Keep `Wordmark` API; mark always uses PNG

### Task 2: Placements

- [x] Splash: large mark (~80)
- [x] Onboarding: small mark (~28) top area
- [x] Login / Role: already use `Wordmark` (inherits)
- [x] Teacher Home: already uses `Wordmark` (+ header on filled home)
- [x] Student Home + Parent Home: add compact light branding in gradient header

### Task 3: Verify

- [x] Dev server shows E1 on listed screens at 28/40/80 sizes
