# Trainee Deep Logic — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Make متدرّب product logic deep and consistent: enroll → pay → learn → progress → certificate + Live wait/attend/rate.

**Files:**
- Create `src/traineeLogic.ts` — enrollment types + pure helpers
- Modify `src/coursesCatalog.ts` — richer filters + optional demo reviews field helpers
- Modify `src/App.tsx` — screens, wiring, TraineeHome, Account

## Task 1: traineeLogic + catalog filters
## Task 2: App state + checkout/progress/certificate screens
## Task 3: Live trainee screens + CourseLearn/Detail/Hub deepen
## Task 4: TraineeHome + Account + back-nav consistency + build verify
